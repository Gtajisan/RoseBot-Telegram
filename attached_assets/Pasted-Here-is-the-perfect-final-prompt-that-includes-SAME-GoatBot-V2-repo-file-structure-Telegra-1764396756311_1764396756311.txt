Here is the **perfect final prompt** that includes:

✅ SAME GoatBot-V2 repo file structure
✅ Telegram Bot conversion
✅ **AND full adapted Telegram API layer** that matches GoatBot-V2’s internal API design
(i.e., `Goat.js`, `/bot/`, `/func/`, `utils.js` will use a custom TG wrapper API designed EXACTLY for this repo structure)

You can copy–paste this into any AI code generator.

---

# ✅ **FINAL MEGA PROMPT — GoatBot-V2 → Telegram Bot WITH ADAPTED API LAYER**

**PROMPT START**

I want you to convert my entire **GoatBot-V2** repository into a fully working **Telegram bot**, but using the **same file structure, same logic, same module system, and same folder names**.

Also generate a **new Telegram API wrapper** that works exactly like GoatBot-V2's internal API functions.

---

# 📦 **SAME EXACT FILE STRUCTURE**

Do **NOT** change the repo structure. Keep everything:

```
/
├── .github/
├── .vscode/
├── bot/
│   ├── api/
│   ├── events/
│   ├── handlers/
│   └── cache/
├── dashboard/
├── database/
├── fb-chat-api/      ← convert to /bot/api/telegram/
├── func/
├── languages/
├── logger/
├── scripts/
│   ├── commands/
│   └── events/
├── .eslintrc.json
├── .gitignore
├── .replit
├── CHANGELOG.md
├── DOCS.md
├── Goat.js
├── config.json
├── configCommands.json
├── index.js
├── package.json
├── utils.js
├── update.js
├── updater.js
├── versions.json
```

---

# 🧠 **OBJECTIVE**

Make a brand-new Telegram bot that:

* Uses **GoatBot-V2 structure**
* Uses **Telegram API wrapper instead of fb-chat-api**
* Has **command loader**, **event loader**, **language loader**
* Works with **config.json** and **configCommands.json**
* Works with **Goat.js** logic but rewritten for Telegram

---

# 🔧 **TECH REQUIREMENTS**

### ✔ Use Node.js

### ✔ Use `node-telegram-bot-api` OR Telegraf

(Choose whichever is best)

---

# 🚀 **ADAPTED TELEGRAM API WRAPPER (IMPORTANT)**

Create a folder:

```
/bot/api/telegram/
```

Inside it, generate API classes matching the Messenger version:

### Must include:

#### **1. message.js**

```
sendMessage(chatId, text, options)
sendReply(chatId, messageId, text)
sendPhoto(...)
sendDocument(...)
sendAudio(...)
editMessage(...)
deleteMessage(...)
```

#### **2. user.js**

```
getUserInfo(userId)
resolveUsername(username)
getProfilePhoto(userId)
```

#### **3. group.js**

```
getChat(chatId)
getGroupAdmins(chatId)
kickMember(chatId, userId)
unbanMember(chatId, userId)
getChatMember(chatId, userId)
```

#### **4. update.js**

* Convert Telegram updates → GoatBot event style

```
{
  type: "message",
  senderID: "...",
  threadID: "...",
  messageID: "...",
  body: "...",
  attachments: [...],
  mentions: [...]
}
```

#### **5. eventAdapter.js**

Convert Telegram events into GoatBot events:

```
message
new_chat_member
left_chat_member
edited_message
callback_query
```

---

# 📘 **ADAPT Goat.js FOR TELEGRAM**

Rewrite Goat.js into **TelegramGoat.js**, but keep the same API:

Must include:

```
sendMessage()
reply()
sendTyping()
getUserInfo()
getThreadInfo()
getThreadAdminIDs()
addCommand()
loadCommands()
loadEvents()
getPrefix()
cooldowns
```

Everything must look like the original GoatBot but run on Telegram.

---

# 📁 **HANDLER SYSTEM (IMPORTANT)**

### `/bot/handlers/commandHandler.js`

* Load all `/scripts/commands/*.js`
* Detect prefix
* Execute
* Cooldowns
* Owner/admin checks
* Language support

### `/bot/handlers/eventHandler.js`

* Load all `/scripts/events/*.js`
* Trigger from Telegram updates

---

# 🌍 **LANGUAGE SYSTEM**

Use `/languages/*.json`

Format example:

```
{
   "help": {
      "title": "Bot Commands",
      "footer": "Use /help <command> for info."
   }
}
```

Auto-load language based on:

```
config.json → language
```

---

# ⚙️ **index.js REQUIREMENTS**

Create a fully working `index.js` that:

1. Loads config
2. Starts Telegram bot
3. Loads Goat.js core
4. Loads Telegram API wrapper
5. Loads commands & events
6. Starts dashboard server
7. Starts bot polling/webhook
8. Logs everything using `/logger/`

---

# 🔮 **CORE BOT FEATURES TO INCLUDE**

### Commands

* /help
* /menu
* /ping
* /info
* /stats
* /owner
* /uptime

### System

* Cooldown system
* Owner-only commands
* Group-only commands
* Private-only commands
* Auto-reload commands
* Auto-update version from versions.json

### Group features

* Welcome
* Leave
* Mention reply
* Admin detection

---

# 📄 **DELIVERABLES YOU MUST GENERATE**

Produce full code for:

* `index.js`
* `bot/api/telegram/*`
* Modified `Goat.js`
* Command handler
* Event handler
* Example commands
* Example events
* utils.js updated
* package.json updated
* README.md updated

Final code must be:

* Clean
* Modular
* Same structure as GoatBot-V2
* 100% Telegram-compatible
* Ready to run with:

```
npm install
node index.js
```

**PROMPT END**

---

If you want, I can now generate:

🔥 Full Telegram API wrapper
🔥 Full Goat.js (Telegram version)
🔥 Full index.js
🔥 Full commands folder

Just tell me: **"generate TelegramGoat.js"** or **"generate full project"**.
